name: Flyway Guard

on:
  pull_request:
    branches: [main, dev]
    paths:
      - 'learning-card/src/main/resources/db/migration/**'
  push:
    branches: [main, dev]
    paths:
      - 'learning-card/src/main/resources/db/migration/**'

permissions:
  contents: read

jobs:
  flyway-guard:
    uses: team-project-final/synapse-shared/.github/workflows/flyway-guard.yml@main
